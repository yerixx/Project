import styled from "styled-components";

export const WorkGalleryBox = styled.section`
  width: 100%;
  height: 100vh;
  background: ${(props) => props.theme.subColor};
`;
export const WorkTitle = styled.article``;
